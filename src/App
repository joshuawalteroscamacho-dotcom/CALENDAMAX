git initimport { useState, useEffect, useRef } from "react";
import Dashboard from "./Dashboard";
import type { AppUser } from "./types";

const NAV_LINKS = [
  { label: "Características", href: "#features" },
  { label: "Beneficios", href: "#benefits" },
  { label: "Testimonios", href: "#testimonials" },
];

const FEATURES = [
  {
    icon: (
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" className="w-7 h-7">
        <rect x="3" y="4" width="18" height="18" rx="2"/>
        <path d="M16 2v4M8 2v4M3 10h18"/>
        <path d="M8 14h.01M12 14h.01M16 14h.01M8 18h.01M12 18h.01M16 18h.01" strokeLinecap="round" strokeWidth="2"/>
      </svg>
    ),
    title: "Calendario Interactivo",
    desc: "Organiza tus tareas, exámenes y deadlines en una vista mensual. Crea y edita eventos con un solo clic.",
  },
  {
    icon: (
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" className="w-7 h-7">
        <circle cx="12" cy="12" r="9"/>
        <path d="M12 7v5l3 3" strokeLinecap="round"/>
        <path d="M6.5 3.5l1.5 1.5M17.5 3.5l-1.5 1.5" strokeLinecap="round"/>
      </svg>
    ),
    title: "Sistema de Alarmas",
    desc: "Configura recordatorios inteligentes para entregas, clases y compromisos. Nunca más olvides una fecha.",
  },
  {
    icon: (
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" className="w-7 h-7">
        <circle cx="12" cy="13" r="8"/>
        <path d="M12 9v4l2.5 2.5" strokeLinecap="round"/>
        <path d="M9 2h6M12 2v3" strokeLinecap="round"/>
      </svg>
    ),
    title: "Focus Zone Pomodoro",
    desc: "Sesiones configurables de concentración profunda con descansos automáticos. Estudia más en menos tiempo.",
  },
];

const BENEFITS = [
  { stat: "87%", label: "de usuarios reducen su procrastinación en la primera semana" },
  { stat: "3×", label: "más productivo que estudiar sin estructura de tiempo" },
  { stat: "0", label: "apps adicionales necesitas. Todo en un solo lugar" },
  { stat: "10s", label: "para entender cómo funciona. Sin curva de aprendizaje" },
];

const TESTIMONIALS = [
  {
    name: "Valentina R.",
    grade: "11° grado · ITC",
    text: "Antes estudiaba el día anterior al examen. Ahora con Calendamax organizo toda la semana y me queda tiempo para salir con mis amigos.",
    avatar: "VR",
  },
  {
    name: "Sebastián M.",
    grade: "10° grado · Colegio Nacional",
    text: "El Pomodoro me cambió la vida. En serio. Estudio 25 minutos, descanso 5, y rindo mucho más que en 2 horas sin concentración.",
    avatar: "SM",
  },
  {
    name: "Camila T.",
    grade: "12° grado · ITC",
    text: "Ya no uso 4 apps distintas. El calendario, las alarmas y el timer están en el mismo lugar. Súper cómodo y visual.",
    avatar: "CT",
  },
];

export default function App() {
  const [user, setUser] = useState<AppUser | null>(null);
  const [formData, setFormData] = useState({ name: "", email: "", grade: "" });
  const [menuOpen, setMenuOpen] = useState(false);
  const [scrolled, setScrolled] = useState(false);

  useEffect(() => {
    const handler = () => setScrolled(window.scrollY > 20);
    window.addEventListener("scroll", handler);
    return () => window.removeEventListener("scroll", handler);
  }, []);

  function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    if (formData.name && formData.email) {
      setUser({ name: formData.name, email: formData.email, grade: formData.grade });
    }
  }

  // Registro con Google: reduce la fricción evitando llenar el formulario manual.
  // TODO: reemplazar por una integración real con Google Identity Services
  // (https://developers.google.com/identity/gsi/web) usando un Client ID propio;
  // este stub simula la respuesta de OAuth para efectos de la demo.
  function handleGoogleSignup() {
    setUser({ name: "Usuario de Google", email: "usuario@gmail.com", grade: "" });
  }

  if (user) {
    return <Dashboard user={user} onLogout={() => setUser(null)} />;
  }

  return (
    <div style={{ fontFamily: "'Instrument Sans', sans-serif" }} className="min-h-full bg-white text-gray-900">

      {/* NAV */}
      <nav className={`fixed top-0 left-0 right-0 z-50 transition-all duration-300 ${scrolled ? "bg-white/95 backdrop-blur-sm border-b border-gray-100 shadow-sm" : "bg-transparent"}`}>
        <div className="max-w-6xl mx-auto px-6 h-16 flex items-center justify-between">
          <a href="#" className="flex items-center gap-2.5">
            <div className="w-8 h-8 bg-[#E8001D] rounded-lg flex items-center justify-center">
              <svg className="w-4 h-4 text-white" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5">
                <circle cx="12" cy="12" r="9"/><path d="M12 8v4l2.5 2.5" strokeLinecap="round"/>
              </svg>
            </div>
            <span style={{ fontFamily: "'Outfit', sans-serif" }} className="text-xl font-bold tracking-tight text-gray-900">
              Calenda<span className="text-[#E8001D]">max</span>
            </span>
          </a>

          <div className="hidden md:flex items-center gap-8">
            {NAV_LINKS.map(l => (
              <a key={l.href} href={l.href} className="text-sm font-medium text-gray-600 hover:text-[#E8001D] transition-colors">
                {l.label}
              </a>
            ))}
          </div>

          <div className="flex items-center gap-3">
            <a href="#register" className="hidden md:inline-flex items-center px-5 py-2 bg-[#E8001D] hover:bg-[#B8001A] text-white text-sm font-semibold rounded-xl transition-colors">
              Empieza gratis
            </a>
            <button onClick={() => setMenuOpen(v => !v)} className="md:hidden p-2 rounded-lg hover:bg-gray-100 transition-colors">
              <svg className="w-5 h-5 text-gray-700" fill="none" stroke="currentColor" strokeWidth="2" viewBox="0 0 24 24">
                {menuOpen ? <path d="M6 18L18 6M6 6l12 12" strokeLinecap="round"/> : <path d="M4 6h16M4 12h16M4 18h16" strokeLinecap="round"/>}
              </svg>
            </button>
          </div>
        </div>

        {menuOpen && (
          <div className="md:hidden bg-white border-t border-gray-100 px-6 py-4 flex flex-col gap-3">
            {NAV_LINKS.map(l => (
              <a key={l.href} href={l.href} onClick={() => setMenuOpen(false)} className="text-sm font-medium text-gray-700 py-1.5">{l.label}</a>
            ))}
            <a href="#register" onClick={() => setMenuOpen(false)} className="mt-1 px-5 py-2.5 bg-[#E8001D] text-white text-sm font-semibold rounded-xl text-center">
              Empieza gratis
            </a>
          </div>
        )}
      </nav>

      {/* HERO */}
      <section className="relative min-h-screen flex items-center overflow-hidden bg-white pt-16">
        <div className="absolute inset-0 pointer-events-none">
          <div className="absolute top-0 right-0 w-1/2 h-full bg-gradient-to-bl from-red-50 to-transparent"/>
          <div className="absolute bottom-0 left-0 w-96 h-96 bg-red-50 rounded-full blur-3xl opacity-40 -translate-x-1/2 translate-y-1/2"/>
        </div>

        <div className="max-w-6xl mx-auto px-6 py-24 grid lg:grid-cols-2 gap-16 items-center w-full">
          <div>
            <div className="inline-flex items-center gap-2 px-3 py-1.5 bg-red-50 rounded-full mb-8">
              <div className="w-1.5 h-1.5 bg-[#E8001D] rounded-full animate-pulse"/>
              <span className="text-[#E8001D] text-xs font-semibold tracking-wide uppercase">Para estudiantes que quieren más</span>
            </div>

            <h1 style={{ fontFamily: "'Outfit', sans-serif" }} className="text-5xl lg:text-6xl font-extrabold leading-[1.05] tracking-tight mb-6 text-gray-900">
              Para de<br/>
              <span className="text-[#E8001D]">procrastinar.</span><br/>
              Empieza hoy.
            </h1>

            <p className="text-lg text-gray-500 leading-relaxed mb-10 max-w-md">
              Calendario, cronograma semanal, alarmas, Pomodoro y carpeta de archivos en una sola pantalla. Sin saltar entre apps. Sin excusas.
            </p>

            <div className="flex flex-col sm:flex-row gap-3">
              <a href="#register" className="inline-flex items-center justify-center gap-2 px-7 py-3.5 bg-[#E8001D] hover:bg-[#B8001A] text-white font-semibold rounded-xl transition-all hover:shadow-lg hover:shadow-red-200 text-base">
                Empieza a organizar tu tiempo
                <svg className="w-4 h-4" fill="none" stroke="currentColor" strokeWidth="2.5" viewBox="0 0 24 24"><path d="M5 12h14M12 5l7 7-7 7" strokeLinecap="round" strokeLinejoin="round"/></svg>
              </a>
              <a href="#features" className="inline-flex items-center justify-center gap-2 px-7 py-3.5 border border-gray-200 hover:border-gray-300 text-gray-700 font-semibold rounded-xl transition-all text-base hover:bg-gray-50">
                Ver cómo funciona
              </a>
            </div>

            <div className="mt-10 flex items-center gap-6 pt-6 border-t border-gray-100">
              <div className="flex -space-x-2">
                {["SM","VR","CT","LP"].map(i => (
                  <div key={i} className="w-8 h-8 rounded-full bg-gradient-to-br from-red-400 to-red-600 border-2 border-white flex items-center justify-center">
                    <span className="text-white text-[9px] font-bold">{i}</span>
                  </div>
                ))}
              </div>
              <div>
                <div className="flex gap-0.5 mb-0.5">
                  {[1,2,3,4,5].map(s => (
                    <svg key={s} className="w-3.5 h-3.5 text-[#E8001D]" fill="currentColor" viewBox="0 0 20 20"><path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/></svg>
                  ))}
                </div>
                <p className="text-xs text-gray-500"><span className="font-semibold text-gray-700">+200 estudiantes</span> ya lo usan</p>
              </div>
            </div>
          </div>

          <div className="hidden lg:block">
            <div className="bg-gray-950 rounded-2xl p-6 shadow-2xl">
              <div className="flex items-center gap-2 mb-4">
                <div className="w-2.5 h-2.5 rounded-full bg-red-500"/>
                <div className="w-2.5 h-2.5 rounded-full bg-yellow-500"/>
                <div className="w-2.5 h-2.5 rounded-full bg-green-500"/>
                <span className="ml-2 text-gray-500 text-xs">calendamax · Focus Zone</span>
              </div>
              <div className="flex flex-col items-center gap-4 py-4">
                <div className="flex gap-2">
                  {["Enfoque","Descanso corto","Descanso largo"].map((m, i) => (
                    <span key={m} className={`px-3 py-1.5 rounded-xl text-xs font-semibold ${i === 0 ? "bg-white text-gray-900" : "text-gray-500"}`}>{m}</span>
                  ))}
                </div>
                <div className="relative w-44 h-44">
                  <svg className="w-44 h-44 -rotate-90" viewBox="0 0 176 176">
                    <circle cx="88" cy="88" r="76" stroke="#222" strokeWidth="10" fill="none"/>
                    <circle cx="88" cy="88" r="76" stroke="#E8001D" strokeWidth="10" fill="none"
                      strokeDasharray={`${2*Math.PI*76}`} strokeDashoffset={`${2*Math.PI*76*0.35}`} strokeLinecap="round"/>
                  </svg>
                  <div className="absolute inset-0 flex flex-col items-center justify-center">
                    <span className="text-[10px] text-[#E8001D] font-bold uppercase tracking-widest mb-1">Enfoque</span>
                    <span style={{ fontFamily: "'Outfit', sans-serif" }} className="text-5xl font-extrabold text-white">16:15</span>
                    <span className="text-gray-600 text-xs mt-1">25 min</span>
                  </div>
                </div>
                <div className="flex gap-3">
                  <div className="px-6 py-2.5 bg-[#E8001D] rounded-xl text-white text-sm font-bold flex items-center gap-2">
                    <svg className="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M6 4h4v16H6V4zm8 0h4v16h-4V4z"/></svg>
                    Pausar
                  </div>
                </div>
                <div className="flex gap-6 text-center mt-2">
                  {[["3","Sesiones"],["75","Min enfocado"],["0","Ciclos"]].map(([n, l]) => (
                    <div key={l}>
                      <p style={{ fontFamily: "'Outfit', sans-serif" }} className="text-xl font-bold text-white">{n}</p>
                      <p className="text-[10px] text-gray-500">{l}</p>
                    </div>
                  ))}
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* STRIP */}
      <div className="bg-[#E8001D] py-4 overflow-hidden">
        <div className="flex gap-16 whitespace-nowrap">
          {["Calendario Interactivo","Cronograma Semanal","Sistema de Alarmas","Focus Zone Pomodoro","Mi Carpeta de Archivos","Sin procrastinación"].map((t, i) => (
            <span key={i} className="text-white/90 text-sm font-medium inline-flex items-center gap-4">
              {t} <span className="text-white/40">·</span>
            </span>
          ))}
          {["Calendario Interactivo","Cronograma Semanal","Sistema de Alarmas","Focus Zone Pomodoro","Mi Carpeta de Archivos","Sin procrastinación"].map((t, i) => (
            <span key={`r-${i}`} className="text-white/90 text-sm font-medium inline-flex items-center gap-4">
              {t} <span className="text-white/40">·</span>
            </span>
          ))}
        </div>
      </div>

      {/* FEATURES */}
      <section id="features" className="py-24 bg-gray-50">
        <div className="max-w-6xl mx-auto px-6">
          <div className="max-w-xl mb-16">
            <span className="text-[#E8001D] text-xs font-semibold tracking-widest uppercase mb-4 block">Herramientas</span>
            <h2 style={{ fontFamily: "'Outfit', sans-serif" }} className="text-4xl font-extrabold text-gray-900 mb-4 leading-tight">
              Todo lo que necesitas<br/>para organizarte
            </h2>
            <p className="text-gray-500 text-lg">Cinco herramientas. Una plataforma. Cero complicaciones.</p>
          </div>

          <div className="grid md:grid-cols-3 gap-6">
            {FEATURES.map((f, i) => (
              <div key={i} className="bg-white border border-gray-200 rounded-2xl p-8 hover:border-red-200 hover:shadow-md transition-all group">
                <div className="w-12 h-12 bg-red-50 group-hover:bg-[#E8001D] text-[#E8001D] group-hover:text-white rounded-xl flex items-center justify-center mb-5 transition-all">
                  {f.icon}
                </div>
                <h3 style={{ fontFamily: "'Outfit', sans-serif" }} className="text-xl font-bold text-gray-900 mb-3">{f.title}</h3>
                <p className="text-gray-500 leading-relaxed text-sm">{f.desc}</p>
              </div>
            ))}
          </div>

          <div className="grid md:grid-cols-2 gap-6 mt-6">
            {[
              { icon: <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" className="w-7 h-7"><path d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" strokeLinecap="round"/><path d="M9 12l2 2 4-4" strokeLinecap="round" strokeLinejoin="round"/></svg>, title: "Cronograma Semanal", desc: "Organiza tus actividades de lunes a domingo, ordenadas de mayor a menor prioridad. Visualiza tu semana completa de un vistazo." },
              { icon: <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" className="w-7 h-7"><path d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-6l-2-2H5a2 2 0 00-2 2z" strokeLinecap="round" strokeLinejoin="round"/></svg>, title: "Mi Carpeta de Archivos", desc: "Sube y organiza tus apuntes, trabajos y documentos. Vincula cada archivo a una tarea específica del calendario." },
            ].map((f, i) => (
              <div key={i} className="bg-white border border-gray-200 rounded-2xl p-8 hover:border-red-200 hover:shadow-md transition-all group">
                <div className="w-12 h-12 bg-red-50 group-hover:bg-[#E8001D] text-[#E8001D] group-hover:text-white rounded-xl flex items-center justify-center mb-5 transition-all">
                  {f.icon}
                </div>
                <h3 style={{ fontFamily: "'Outfit', sans-serif" }} className="text-xl font-bold text-gray-900 mb-3">{f.title}</h3>
                <p className="text-gray-500 leading-relaxed text-sm">{f.desc}</p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* BENEFITS */}
      <section id="benefits" className="py-24 bg-[#E8001D]">
        <div className="max-w-6xl mx-auto px-6">
          <div className="max-w-xl mb-16">
            <span className="text-red-200 text-xs font-semibold tracking-widest uppercase mb-4 block">Resultados</span>
            <h2 style={{ fontFamily: "'Outfit', sans-serif" }} className="text-4xl font-extrabold text-white mb-4 leading-tight">
              Números que<br/>hablan solos
            </h2>
            <p className="text-red-100 text-lg">Lo que los estudiantes logran con Calendamax en la primera semana.</p>
          </div>
          {/* Bordes redondeados homogeneizados (rounded-2xl) en todas las tarjetas
              de esta sección de estadísticas, igual que en Features y Testimonios,
              para mantener proporciones idénticas en toda la landing. */}
          <div className="grid grid-cols-2 lg:grid-cols-4 gap-6">
            {BENEFITS.map((b, i) => (
              <div key={i} className="bg-white/10 backdrop-blur border border-white/20 rounded-2xl p-6">
                <div style={{ fontFamily: "'Outfit', sans-serif" }} className="text-5xl font-extrabold text-white mb-3">{b.stat}</div>
                <p className="text-red-100 text-sm leading-relaxed">{b.label}</p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* TESTIMONIALS */}
      <section id="testimonials" className="py-24 bg-white">
        <div className="max-w-6xl mx-auto px-6">
          <div className="max-w-xl mb-16">
            <span className="text-[#E8001D] text-xs font-semibold tracking-widest uppercase mb-4 block">Testimonios</span>
            <h2 style={{ fontFamily: "'Outfit', sans-serif" }} className="text-4xl font-extrabold text-gray-900 mb-4 leading-tight">
              Lo que dicen<br/>los estudiantes
            </h2>
            <p className="text-gray-500 text-lg">Reales. Sin filtros. Igual que vos.</p>
          </div>
          <div className="grid md:grid-cols-3 gap-6">
            {TESTIMONIALS.map((t, i) => (
              <div key={i} className="border border-gray-200 rounded-2xl p-6 hover:shadow-md transition-all">
                <div className="flex gap-0.5 mb-4">
                  {[1,2,3,4,5].map(s => (
                    <svg key={s} className="w-4 h-4 text-[#E8001D]" fill="currentColor" viewBox="0 0 20 20"><path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/></svg>
                  ))}
                </div>
                <p className="text-gray-700 text-sm leading-relaxed mb-6">"{t.text}"</p>
                <div className="flex items-center gap-3 pt-4 border-t border-gray-100">
                  <div className="w-9 h-9 bg-gradient-to-br from-red-400 to-red-700 rounded-full flex items-center justify-center flex-shrink-0">
                    <span className="text-white text-xs font-bold">{t.avatar}</span>
                  </div>
                  <div>
                    <p className="text-sm font-semibold text-gray-900">{t.name}</p>
                    <p className="text-xs text-gray-500">{t.grade}</p>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* REGISTER */}
      <section id="register" className="py-24 bg-gray-950">
        <div className="max-w-6xl mx-auto px-6 grid lg:grid-cols-2 gap-16 items-center">
          <div>
            <span className="text-red-400 text-xs font-semibold tracking-widest uppercase mb-4 block">Registro Gratuito</span>
            <h2 style={{ fontFamily: "'Outfit', sans-serif" }} className="text-4xl font-extrabold text-white mb-4 leading-tight">
              Tu tiempo vale.<br/>
              <span className="text-[#E8001D]">Empezá a cuidarlo.</span>
            </h2>
            <p className="text-gray-400 text-lg mb-8 leading-relaxed">
              Crea tu cuenta en segundos y accede de inmediato a todas las herramientas.
            </p>
            <div className="flex flex-col gap-3">
              {["Calendario personal con tareas y prioridades","Cronograma semanal organizado","Alarmas ilimitadas configurables","Focus Zone Pomodoro personalizable","Mi Carpeta para tus archivos y apuntes"].map((f, i) => (
                <div key={i} className="flex items-center gap-3">
                  <div className="w-5 h-5 bg-[#E8001D] rounded-full flex items-center justify-center flex-shrink-0">
                    <svg className="w-3 h-3 text-white" fill="none" stroke="currentColor" strokeWidth="3" viewBox="0 0 24 24"><path d="M5 13l4 4L19 7" strokeLinecap="round" strokeLinejoin="round"/></svg>
                  </div>
                  <span className="text-gray-300 text-sm">{f}</span>
                </div>
              ))}
            </div>
          </div>

          <div className="bg-white rounded-2xl p-8">
            <h3 style={{ fontFamily: "'Outfit', sans-serif" }} className="text-2xl font-bold text-gray-900 mb-6">Crear cuenta gratis</h3>

            <button
              type="button"
              onClick={handleGoogleSignup}
              className="w-full py-3.5 bg-white border border-gray-200 hover:bg-gray-50 text-gray-700 font-semibold rounded-xl transition-all flex items-center justify-center gap-3 text-base"
            >
              <svg className="w-5 h-5 flex-shrink-0" viewBox="0 0 24 24">
                <path fill="#4285F4" d="M23.766 12.276c0-.818-.074-1.606-.212-2.364H12.24v4.475h6.482a5.54 5.54 0 01-2.402 3.632v3.017h3.886c2.274-2.093 3.56-5.176 3.56-8.76z" />
                <path fill="#34A853" d="M12.24 24c3.24 0 5.956-1.075 7.942-2.908l-3.886-3.017c-1.078.724-2.46 1.15-4.056 1.15-3.12 0-5.76-2.107-6.705-4.938H1.51v3.11A11.997 11.997 0 0012.24 24z" />
                <path fill="#FBBC05" d="M5.535 14.287a7.19 7.19 0 010-4.573V6.604H1.51a12.002 12.002 0 000 10.792l4.025-3.11z" />
                <path fill="#EA4335" d="M12.24 4.775c1.762 0 3.344.605 4.588 1.793l3.443-3.443C18.192 1.19 15.48 0 12.24 0 7.523 0 3.443 2.688 1.51 6.604l4.025 3.11c.945-2.831 3.585-4.939 6.705-4.939z" />
              </svg>
              Registrarse con Google
            </button>

            <div className="flex items-center gap-3 my-5">
              <div className="flex-1 h-px bg-gray-200" />
              <span className="text-xs text-gray-500 font-medium">o continúa con tu email</span>
              <div className="flex-1 h-px bg-gray-200" />
            </div>

            <form onSubmit={handleSubmit} className="flex flex-col gap-4">
              <div>
                <label className="block text-sm font-medium text-gray-700 mb-1.5">Nombre completo</label>
                <input
                  type="text"
                  placeholder="Sebastián López"
                  value={formData.name}
                  onChange={e => setFormData({ ...formData, name: e.target.value })}
                  required
                  className="w-full px-4 py-3 border border-gray-200 rounded-xl text-sm focus:outline-none focus:ring-2 focus:ring-[#E8001D] focus:border-transparent transition-all"
                />
              </div>
              <div>
                <label className="block text-sm font-medium text-gray-700 mb-1.5">Correo electrónico</label>
                <input
                  type="email"
                  placeholder="sebastian@estudiante.com"
                  value={formData.email}
                  onChange={e => setFormData({ ...formData, email: e.target.value })}
                  required
                  className="w-full px-4 py-3 border border-gray-200 rounded-xl text-sm focus:outline-none focus:ring-2 focus:ring-[#E8001D] focus:border-transparent transition-all"
                />
              </div>
              <div>
                <label className="block text-sm font-medium text-gray-700 mb-1.5">Grado / Curso <span className="text-gray-500 font-normal">(opcional)</span></label>
                <select
                  value={formData.grade}
                  onChange={e => setFormData({ ...formData, grade: e.target.value })}
                  className="w-full px-4 py-3 border border-gray-200 rounded-xl text-sm focus:outline-none focus:ring-2 focus:ring-[#E8001D] focus:border-transparent transition-all bg-white text-gray-700"
                >
                  <option value="">Seleccionar grado</option>
                  {["8° grado","9° grado","10° grado","11° grado","12° grado","Universidad"].map(g => (
                    <option key={g} value={g}>{g}</option>
                  ))}
                </select>
              </div>
              <button type="submit" className="w-full py-3.5 bg-[#E8001D] hover:bg-[#B8001A] text-white font-semibold rounded-xl transition-all hover:shadow-lg hover:shadow-red-200 text-base mt-1">
                Empieza a organizar tu tiempo →
              </button>
              <p className="text-center text-xs text-gray-500">Acceso inmediato · Sin verificación · Completamente gratis</p>
            </form>
          </div>
        </div>
      </section>

      {/* FOOTER */}
      <footer className="bg-gray-900 border-t border-gray-800 py-10">
        <div className="max-w-6xl mx-auto px-6 flex flex-col md:flex-row items-center justify-between gap-6">
          <div className="flex items-center gap-2.5">
            <div className="w-7 h-7 bg-[#E8001D] rounded-lg flex items-center justify-center">
              <svg className="w-3.5 h-3.5 text-white" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5">
                <circle cx="12" cy="12" r="9"/><path d="M12 8v4l2.5 2.5" strokeLinecap="round"/>
              </svg>
            </div>
            <span style={{ fontFamily: "'Outfit', sans-serif" }} className="text-lg font-bold text-white">
              Calenda<span className="text-[#E8001D]">max</span>
            </span>
          </div>
          <div className="flex gap-6">
            {NAV_LINKS.map(l => (
              <a key={l.href} href={l.href} className="text-sm text-gray-500 hover:text-gray-300 transition-colors">{l.label}</a>
            ))}
          </div>
          <p className="text-xs text-gray-600">© 2026 Calendamax. Todos los derechos reservados.</p>
        </div>
      </footer>
    </div>
  );
}
